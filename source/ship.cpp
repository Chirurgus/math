#include <iostream>

#include "matrix.h"

using namespace std;

int blankinship() {

	/*matrix<long> mtx(3,1);
	matrix<long> mtx1(3, 1);

	int a, b;
	cout << "Enter two integers, seperated by a new line" << endl;
	cin >> a;
	cin >> b;

	mtx[0][0] = 1;	mtx1[0][0] = 0;
	mtx[1][0] = 0;	mtx1[1][0] = 1;
	mtx[2][0] = a;	mtx1[2][0] = b;


	while (mtx1[2][0] != 0 && mtx[2][0] != 0) {
		if (mtx[2][0] >= mtx1[2][0])
			mtx = mtx - mtx1;
		else
			mtx1 = mtx1 - mtx;
	}

	if(mtx[2][0]==0){
		mtx = mtx1;
	}

cout << "gcd is:" << mtx[2][0] << endl;
cout << "also, the Bezout coefficients are:" << mtx[0][0] << ", " << mtx[1][0] << endl;

*/
	return 0;
}

